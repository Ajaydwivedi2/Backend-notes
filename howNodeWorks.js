"domain name is not the real address of the server that we access";
"TCP/IP defines how data moves across the internet.";
"HTTP is also an communication protocol.";
`Difference between HTTP and HTTPS is:
1. HTTP is used to communicate with the server.
2. HTTPS is encrypted using TLS/SSL.
The first job of TCP is to break the request and responses into thousands of small chunks before they are sent. and once they get their destination they will be reassembled into their original state
#. The job of the IP protocol is to send and route all of these packets through the internet to their destination using IP addresses on each packets.
`;

("V8 engine converts javascript code into mechine code that a machine can understand");
("Event loop has many phases where each phases has its own callback queue.");
(" One tick means one cycle of the event loop");
("Backpressure is the problem that occurs when reading data in chunks from the file system happens so fast that the writtable stream can't write that fast");
("'Pipe operator automacilly handles the spped of the data coming in and going out through the stream.");
("We can drop the .js extension when requiring a module.");
("results are stored somewhere in the node processor cache");
